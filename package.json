{
  "name": "nucleo-mcp",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "bun run src/index.ts",
    "dev": "bun --watch src/index.ts",
    "credential": "bun run scripts/credential.ts",
    "format": "dprint fmt",
    "lint": "oxlint src/ scripts/",
    "check": "oxlint src/ scripts/ && dprint check && tsc --noEmit"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0",
    "drizzle-orm": "^1.0.0-beta.21",
    "fflate": "^0.8.2",
    "zod": "^3.25.0"
  },
  "devDependencies": {
    "@types/bun": "^1.3.12",
    "dprint": "^0.54.0",
    "oxlint": "^1.60.0",
    "typescript": "^5.7.0"
  }
}
